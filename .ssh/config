Host *
    TCPKeepAlive no
    ServerAliveInterval 60 

GSSAPIAuthentication no

#When opening multiple ssh sessions to 1 machine, use the same channel for
#performance
ControlMaster auto
ControlPath /tmp/ssh_mux_%h_%p_%r
